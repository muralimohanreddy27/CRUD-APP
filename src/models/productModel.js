import mongoose from "mongoose";

const productSchema = mongoose.Schema({
    productName : {
        type : String,
        required : true
    },
    image : {
        type : image,
        required : true,
    },
    description : {
        type : String,
        
    }

})